export interface Params {
  makeId: string;
  year: string;
}

export interface ICarsItems {
  Make_ID: number;
  Make_Name: string;
  Model_ID: number;
  Model_Name: string;
}

export interface ICarItem {
  makeName: string;
  modelName: string;
  modelId: number;
  year: string;
}
